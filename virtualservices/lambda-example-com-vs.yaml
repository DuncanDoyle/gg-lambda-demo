apiVersion: gateway.solo.io/v1
kind: VirtualService
metadata:
  name: api-example-com
  namespace: gloo-system
  labels:
    app: quarkus-lambda-ddoyle
spec:
  # sslConfig:
  #   # parameters:
  #   #   cipherSuites:
  #   #   - ECDHE-ECDSA-AES128-GCM-SHA256
  #   #   - ECDHE-RSA-AES128-GCM-SHA256
  #   #   - ECDHE-ECDSA-AES256-GCM-SHA384
  #   #   - ECDHE-RSA-AES256-GCM-SHA384
  #   #   - ECDHE-ECDSA-CHACHA20-POLY1305
  #   #   - ECDHE-RSA-CHACHA20-POLY1305
  #   #   minimumProtocolVersion: TLSv1_2
  #   secretRef:
  #     name: lambda-example-com
  #     namespace: gloo-system
  #   sniDomains:
  #   - 'lambda.example.com'
  virtualHost:
    domains:
    - 'lambda.example.com'
    routes:
    - matchers:
      # - prefix: /test/
      - prefix: /
      routeAction:
        single:
          destinationSpec:
            aws:
              # logicalName: QuarkusLambdaDdoyle
              # logicalName: quarkus-lambda-app-QuarkusLambdaRestDdoyle-NTZDoZz6RBrS
              # logicalName: quarkus-lambda-429-test-QuarkusLambdaTest-Jf88L9aFqkAScd
              # logicalName: quarkus-lambda-app-QuarkusLambdaRestDdoyle-4Kwo4VU1priT
              # logicalName: greeting-function-QuarkusLambdaDdoyle-2CIg7Nh54XUQ
              logicalName: echo
              wrapAsApiGateway: true
              unwrapAsApiGateway: true
          upstream:
            name: my-aws-lambda-upstream
            namespace: gloo-system
      # options:
      #   headerManipulation:
      #     requestHeadersToRemove:
      #     - ":authority"
        # prefixRewrite: "/test"
        # regexRewrite:
        #   pattern:
        #     regex: "^/test(.*)"
        #   substitution: '\1'

        # ddoyle: Implement path prefix replacement using staged transformation.
        # stagedTransformations:
        #   regular:
        #     requestTransforms:
        #     - requestTransformation:
        #         transformationTemplate:
        #           extractors:
        #             lambdaPathExtractor:
        #               header: ':path'
        #               regex: '^\/test(.*)'
        #               subgroup: 1
        #           headers:
        #             :path:
        #               text: "{{ lambdaPathExtractor }}"
        # ddoyle: This doesn't work, because we can't seem to set pseudo headers:
        # * Route Error: ProcessingError. Reason: *headers.plugin: :-prefixed headers cannot be set: ':path'. Route Name: gloo-system_api-example-com-route-0-matcher-0; Route Error: ProcessingError. Reason: *headers.plugin: :-prefixed headers cannot be set: ':path'. Route Name: gloo-system_api-example-com-route-0-matcher-0
        # headerManipulation:
        #   responseHeadersToAdd:
        #   - header:
        #       key: ":path"
        #       value: "/vertx/hello"